# Shared Schemas

Machine-readable definitions shared between the FastAPI backend and the Next.js frontend. Schemas mirror the structures described in `SPEC.md` to keep AI prompts and validations consistent.
